#端口号
spring:
  profiles:
    active: test
  application:
    name: spring-cloud-config-service
  cloud:
    config:
      label: in-test
      profile: test
      name: spring-book-service
server:
  port: 8883

test:
  user:
    name: in-test
