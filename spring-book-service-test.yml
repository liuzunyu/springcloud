#端口号
spring:
  profiles:
    active: test
  application:
    name: spring-cloud-config-service
  cloud:
    config:
      label: master
      profile: test
      name: spring-book-service
server:
  port: 8880

test:
  user:
    name: in-dev-test
